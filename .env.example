# Reverb's public app key. Not a secret: it ships to every client and is
# visible in the web bundle. Copy it from the backend's REVERB_APP_KEY, or
# read it with: php artisan tinker --execute="echo config('broadcasting.connections.reverb.key');"
EXPO_PUBLIC_REVERB_APP_KEY=
